<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <classAccesses>
        <apexClass>LoginRouterService</apexClass>
        <enabled>true</enabled>
    </classAccesses>
    <description>Assign to the Experience Cloud site Guest User. Grants Apex only; admins must add least-privilege read to User and PermissionSetAssignment (and FLS) per org security review so discover() can run.</description>
    <hasActivationRequired>false</hasActivationRequired>
    <label>Login Router Guest</label>
</PermissionSet>
