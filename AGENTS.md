# Critical Instructions for ALL Work

1. My first priority is code reliability.
2. My second priority is making this code as simple and streamlined as possible. Consider how changes will create code bloat and do you best to avoid it. Refactor sections if you notice a better alternative. 
5. When debugging, always think deeply to identify the root cause of the bug. NEVER simply apply a bandaid fix. Don't just short-circuit existing logic. Instead, consider how things can be refactored so that the fix is baked into the core logic.
6. After making changes to the repo, update files in the 'documentation' folder so that they stay aligned with the code. Always consult the file 'Documentation Spec.md' when making updates to documentation files and follow its instructions.